#include <stdio.h>
#include <stdlib.h>
#include <math.h>


int
main(int argc, char *argv[])
{

//    if (argc != 2) {
//        fprintf(stderr, "Usage: %s n\n", argv[0]);
//        exit(1);
//    }
    //long sin_value = atol(argv[1]);
    //long sin_value = atol(1);
    //if (sin_value != NULL) {
        printf("sin_value %d\n", sin(15));
    //}
    exit(0);
}

